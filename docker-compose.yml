services:
  downloader:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./downloads:/app/downloads
      - ./sqlite.db:/app/sqlite.db
    environment:
      - ONEFICHIER_API_KEY=${ONEFICHIER_API_KEY}
      - DOWNLOAD_DIR=/app/downloads
    restart: unless-stopped
